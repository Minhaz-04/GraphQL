# GraphQL
Mini Project using Anilist API
A beginner‑friendly GraphQL mini project that wraps the AniList GraphQL API using Apollo Server.
Here you can search for anime title and ids.
To run this you need node js 18 or above version
Download the repository install dependencies like npm install 
